#include <stdio.h>

/**
 * main - Entry point
 * Description: Program to print single numbers of base 10,
 * followed by  anewline.
 * Return: 0
 */

int main(void)
{
	for (int a; a < 10; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
